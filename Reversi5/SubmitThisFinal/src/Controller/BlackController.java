package Controller;

import model.Cell;
import model.HexBoard;
import model.Player;
import view.Panel;

public class BlackController extends Controller {
  public BlackController(HexBoard model, Player player, Panel view) {
    super(model, player, view);
  }
}
